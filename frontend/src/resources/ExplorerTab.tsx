import type { ResTabProps } from './shared';
import { EmptyState } from '../dashboards/shared';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function ExplorerTab(_: ResTabProps) {
  return <EmptyState text="Coming soon" />;
}
